PORT=3333
HOST=0.0.0.0
CORS_ORIGIN=http://localhost:5173
JWT_SECRET=change_this_secret
JWT_EXPIRES_IN=1h

# NOTE: MongoDB URL was not found in the visible Auth.zip files.
# Replace with your provided cluster URL and only change database name.
MONGODB_URI=mongodb+srv://user:password@cluster0.mongodb.net/empire_underworld?retryWrites=true&w=majority
